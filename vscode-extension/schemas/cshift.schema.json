{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "CShift project (cshift.json)",
	"description": "Project file for `cshiftc build` / `cshiftc run`. Only \"name\" is required.",
	"type": "object",
	"required": ["name"],
	"additionalProperties": false,
	"properties": {
		"$schema": {
			"type": "string"
		},
		"name": {
			"type": "string",
			"pattern": "^[A-Za-z0-9_.-]+$",
			"description": "Project name. Used for the default output file (bin/<name>) and as module name."
		},
		"version": {
			"type": "string",
			"description": "Version of the project (informational)."
		},
		"type": {
			"enum": ["executable", "object"],
			"default": "executable",
			"description": "\"executable\" links a program, \"object\" only produces an object file."
		},
		"sources": {
			"type": "array",
			"items": { "type": "string" },
			"default": ["src"],
			"description": "Source files or directories (searched recursively for *.csh), relative to this file. All files form one program."
		},
		"output": {
			"type": "string",
			"description": "Output path relative to this file, without extension (.exe is added on Windows). Default: bin/<name>."
		},
		"optimize": {
			"type": "integer",
			"minimum": 0,
			"maximum": 3,
			"default": 2,
			"description": "Optimization level 0-3 (like -O0 ... -O3)."
		},
		"links": {
			"type": "array",
			"items": { "type": "string" },
			"description": "Libraries passed to the linker (like -l<name>), in addition to 'link \"name\";' directives in the sources."
		},
		"target": {
			"type": "string",
			"description": "Target triple, e.g. x86_64-w64-windows-gnu. Default: the host."
		}
	}
}
