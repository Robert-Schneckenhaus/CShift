# Files skipped by selfhost/compare.sh: they use syntax the CShift front end (selfhost/src/Syntax) does not know
# yet, so it cannot tokenize/parse them the same way the C++ front end does - that is an expected difference, not
# a regression. One path per line, relative to the repository root; '#' starts a comment.

